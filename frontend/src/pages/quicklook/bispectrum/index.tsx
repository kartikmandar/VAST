import React from 'react';
import { Box, Typography } from '@mui/material';

const UbispectrumPage: React.FC = () => {
    return (
        <Box>
            <Typography variant="h4">Bispectrum Analysis</Typography>
            <Typography variant="body1">Bispectrum Analysis functionality to be done</Typography>
        </Box>
    );
};

export default UbispectrumPage;
