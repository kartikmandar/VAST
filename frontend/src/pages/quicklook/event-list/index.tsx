import React from 'react';
import { Box, Typography } from '@mui/material';

const EventListPage: React.FC = () => {
    return (
        <Box>
            <Typography variant="h4">Event List Analysis</Typography>
            <Typography variant="body1">Event List Analysis functionality to be done</Typography>
        </Box>
    );
};

export default EventListPage; 