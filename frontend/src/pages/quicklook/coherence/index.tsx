import React from 'react';
import { Box, Typography } from '@mui/material';

const UcoherencePage: React.FC = () => {
    return (
        <Box>
            <Typography variant="h4">Coherence Analysis</Typography>
            <Typography variant="body1">Coherence Analysis functionality to be done</Typography>
        </Box>
    );
};

export default UcoherencePage;
