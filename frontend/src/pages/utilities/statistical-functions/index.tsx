import React from 'react';
import { Box, Typography } from '@mui/material';

const UstatisticalUfunctionsPage: React.FC = () => {
    return (
        <Box>
            <Typography variant="h4">Statistical Functions</Typography>
            <Typography variant="body1">Statistical Functions functionality to be done</Typography>
        </Box>
    );
};

export default UstatisticalUfunctionsPage;
