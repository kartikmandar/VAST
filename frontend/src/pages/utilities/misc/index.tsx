import React from 'react';
import { Box, Typography } from '@mui/material';

const UmiscPage: React.FC = () => {
    return (
        <Box>
            <Typography variant="h4">Miscellaneous Utilities</Typography>
            <Typography variant="body1">Miscellaneous Utilities functionality to be done</Typography>
        </Box>
    );
};

export default UmiscPage;
