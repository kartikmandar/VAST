import React from 'react';
import { Box, Typography } from '@mui/material';

const UioPage: React.FC = () => {
    return (
        <Box>
            <Typography variant="h4">I/O Functionality</Typography>
            <Typography variant="body1">I/O Functionality functionality to be done</Typography>
        </Box>
    );
};

export default UioPage;
