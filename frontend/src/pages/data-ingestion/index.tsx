import React from 'react';
import { Box, Typography } from '@mui/material';

const DataIngestionPage: React.FC = () => {
    return (
        <Box>
            <Typography variant="h4">Data Ingestion</Typography>
            <Typography variant="body1">Data Ingestion functionality to be done</Typography>
        </Box>
    );
};

export default DataIngestionPage; 